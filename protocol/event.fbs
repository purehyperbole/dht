include "find_node.fbs";
include "find_value.fbs";
include "store.fbs";
include "types.fbs";

namespace Kademlia;

union Any { FindNode, FindValue, Store }

table Event {
  id:      [ubyte];
  sender:  [ubyte];
  event:   EventType;
  payload: Any;
}

root_type Event;